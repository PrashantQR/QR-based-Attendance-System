# Used when you run `npm run build` (including APK / Capacitor sync).
# Points the app at your deployed API so the native WebView does not rely on defaults.
REACT_APP_API_BASE_URL=https://qr-based-attendance-system-sfdt.onrender.com
